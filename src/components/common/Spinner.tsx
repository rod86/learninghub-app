import {ImSpinner8} from 'react-icons/im';

const Spinner = () => {
    return (
        <div className="flex justify-center">
            <ImSpinner8 className="text-6xl text-emerald-400 animate-spin" />
        </div>
    );
};

export default Spinner;